apiVersion: v1
kind: ConfigMap
metadata:
  name: amcart-frontend-config
  namespace: amcart-frontend
  labels:
    app: amcart-frontend
data:
  # API backend URL (using Kubernetes service name)
  REACT_APP_API_URL: "https://amcart-backend.c-418bb73.kyma.ondemand.com/api"
  # Environment type
  REACT_APP_ENV: "production"